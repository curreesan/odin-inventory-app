<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <h1><%= genre.name %> Games</h1>

  <p><a href="/">← Back to all genres</a></p>

  <% if (games && games.length > 0) { %>
    <ul>
      <% games.forEach(game => { %>
        <li>
          <%= game.name %> - <%= game.cost %>
          <!-- We'll add edit/delete links later -->
        </li>
      <% }); %>
    </ul>
  <% } else { %>
    <p>No games in this genre yet.</p>
  <% } %>

  <p><a href="#">Add New Game to this genre (coming soon)</a></p>
</body>
</html>