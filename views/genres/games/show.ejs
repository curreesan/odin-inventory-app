<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/style.css">
  <title><%= title %></title>
</head>
<body>
  <h1><%= game.name %></h1>

  <p><strong>Cost:</strong> <%= game.cost %></p>
  <p><strong>Genre:</strong> <%= game.genre_name %></p>

  <p>
    <a href="/genres/<%= genreId %>">← Back to <%= game.genre_name %> games</a>
  </p>

  <p>
    <form action="/genres/<%= genreId %>/games/<%= game.id %>/delete" method="POST">
      <button type="submit" onclick="return confirm('Are you sure you want to delete <%= game.name %>?')">Delete Game</button>
    </form>
  </p>
</body>
</html>